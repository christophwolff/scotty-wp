# Scotty – A WordPress Theme based on React and Redux


Set the `ROOT_URL` to your WordPress Install in ./config/config.js.

```
	> npm install
	> npm start
```

Production-Build:

```
	> npm run build
```
